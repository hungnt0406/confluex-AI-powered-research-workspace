- 2026-05-16T11:29:49+0700: Verified Writer Workspace features (Prompt-echo revision, section metadata, and shortcuts) via browser tests.
- 2026-05-17T13:43:27+07:00: Fixed Writer auto-search Tavily 400-char limit overflow. `_build_section_source_query` was truncating to 500 chars; Tavily's API rejects anything over 400, causing every Writer section draft on a long topic/outline to fail auto-search. With no attached papers the section agent then returned "No source papers attached — cannot draft grounded content." Capped query at 380 chars (`backend/services/writer_documents.py:1714`).
- 2026-05-17T13:56:05+07:00: Fixed Writer chat side-tab click. onMouseDown unconditionally set `chatButtonDragRef.current`, and the post-mouseup setTimeout(0) cleanup ran after the synchronous `click` event, so the click guard always blocked the toggle. Added a `didDrag` flag that only flips true after a 4px drag threshold; pure clicks now open chat while drags still skip the toggle (`frontend/components/WriterWorkspace.tsx:1157`).
- 2026-05-17T14:14:18+07:00: Fixed stale Sources panel after Writer draft generation. The draft endpoint auto-attaches papers from Tavily/arXiv but its response only carries the section, not the updated document. `WriterQuestionsPanel.handleDraftSection` was only patching the section in local state, leaving `document.source_papers` and `source_paper_ids_json` stale until a manual refresh. Added an optional `onAfterDraft` prop and wired it to `WriterWorkspace.refreshDocument` so the Sources tab reflects the auto-attached papers immediately (`frontend/components/WriterQuestionsPanel.tsx`, `frontend/components/WriterWorkspace.tsx`).

## 2026-05-17T18:13:26+07:00 — Landing page scroll-reveal animations

Added a lightweight scroll-reveal effect to the landing page using IntersectionObserver, no animation library.

- New hook `frontend/components/landing/useRevealOnScroll.ts` scans `[data-reveal]` elements inside `.landing-root` and toggles `is-revealed` when they enter the viewport (threshold 0.12, rootMargin `-8%` bottom). Disconnects on unmount, and short-circuits to instantly reveal all elements when `prefers-reduced-motion: reduce` matches.
- `LandingPage.tsx` calls the hook once at top level. `data-reveal` is applied to: hero left column, hero `ChatPreview` (delay 1), video section head + frame (delay 1), sources head, each source cell (staggered 0–3), sample head, sample doc article (delay 1), pricing head, each plan card (staggered 0–3), and the footer CTA wrap.
- `frontend/app/landing.css` defines `.landing-root [data-reveal]` (opacity 0 + 22px translateY) and `.is-revealed` for the resting state. Transition: 700ms cubic-bezier(0.22, 0.61, 0.36, 1) on opacity + transform. Stagger via `data-reveal-delay="N"` mapping to `--reveal-step` (90ms per step). Reduced-motion media query disables transition + offset.
- Verified with `npx tsc --noEmit` (clean).
